<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm1="clr-namespace:GymApp2.ViewModel"
             x:Class="GymApp.Views.Profile">

    <ContentView.BindingContext>
        <vm1:ProfileViewModel/>
    </ContentView.BindingContext>
    
    <ContentView.Content>
        <StackLayout>
            <Label Text="{Binding Username}" TextColor="AliceBlue"/>
            <Label Text="{Binding Age}" TextColor="AliceBlue"/>
            <Label Text="{Binding Height}" TextColor="AliceBlue"/>
            <Label Text="{Binding Weight}" TextColor="AliceBlue"/>
            <Label Text="{Binding Score}" TextColor="AliceBlue"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>